import React from 'react';

const Spinner = () => (
  <div className="spinner-border" role="status" style={{marginTop: '24px'}}>
    <span className="visually-hidden">Завантажую...</span>
  </div>
);

export default Spinner;
